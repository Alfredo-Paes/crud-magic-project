spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/crud_magic_project?useTimezone=true&serverTimezone=UTC
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  spring-doc:
    paths-to-match:
      - /api/**/v1/**
    swagger-ui:
      use-root-path: true

